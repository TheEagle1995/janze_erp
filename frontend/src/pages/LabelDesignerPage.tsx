// This page has been retired. Label printing is now handled directly
// from the Products page via the "Label" button on each product card.
export default function LabelDesignerPage() { return null }
